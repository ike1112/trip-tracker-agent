{
  "name": "strands-agent-on-lambda",
  "version": "0.1.0",
  "license": "MIT",
  "bin": {
    "trip-tracker": "bin/trip-tracker.js",
    "strands-agent-on-lambda": "bin/strands-agent-on-lambda.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "npm run test:jest",
    "test:jest": "jest",
    "test:node": "node --test lambdas/*/tests/*.test.js"
  },
  "devDependencies": {
    "aws-cdk": "2.1016.1",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "aws-cdk-lib": "2.196.0",
    "constructs": "^10.0.0"
  }
}
